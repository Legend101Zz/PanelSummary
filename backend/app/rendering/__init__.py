"""Rendering adapters.

Rendering code receives already-authored story artifacts and converts them into
engine-specific shapes. It does not write the story. Renderer goblins stay in
renderer land.
"""
